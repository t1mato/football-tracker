version: 2

models:
  - name: dim_competitions
    columns:
      - name: competition_id
        tests: [unique, not_null]
      - name: competition_code
        tests: [unique, not_null]

  - name: dim_seasons
    columns:
      - name: season_id
        tests: [unique, not_null]

  - name: dim_players
    columns:
      - name: player_id
        tests: [unique, not_null]
      - name: team_id
        tests: [not_null]
      - name: is_derived
        tests: [not_null]

  - name: dim_venues
    columns:
      - name: venue_key
        tests: [unique, not_null]
      - name: venue_name
        tests: [unique, not_null]

  - name: dim_teams
    columns:
      - name: team_id
        tests: [unique, not_null]
      - name: is_derived
        tests: [not_null]

  - name: dim_date
    columns:
      - name: date_day
        tests: [unique, not_null]

  - name: fct_matches
    columns:
      - name: match_id
        tests: [unique, not_null]
      - name: home_team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: away_team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: venue_key
        # No not_null here -- legitimately null for the two unresolved
        # venues and any derived team with no known venue_name. relationships
        # already skips null values (verified against dbt's builtin macro
        # source), so this only checks the venue_keys that are populated.
        tests:
          - relationships:
              arguments:
                to: ref('dim_venues')
                field: venue_key
      - name: season_id
        tests:
          - relationships:
              arguments:
                to: ref('dim_seasons')
                field: season_id
      - name: competition_code
        tests:
          - relationships:
              arguments:
                to: ref('dim_competitions')
                field: competition_code
      - name: kickoff_date_utc
        tests:
          - relationships:
              arguments:
                to: ref('dim_date')
                field: date_day

  - name: fct_team_matches
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [match_id, team_id]
    columns:
      - name: match_id
        tests: [not_null]
      - name: team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: opponent_team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: result
        # No `not_null` here -- null is the correct value for an unfinished
        # match. No `where` filter needed either: accepted_values compiles to
        # `value_field not in (...)`, and NULL NOT IN (...) evaluates to NULL
        # under SQL's three-valued logic, which the test's WHERE clause
        # already excludes (verified against the installed macro source).
        tests:
          - accepted_values:
              arguments:
                values: ['W', 'D', 'L']

  - name: fct_standings_snapshot
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [competition_code, season_id, team_id, snapshot_date]
    columns:
      - name: competition_code
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_competitions')
                field: competition_code
      - name: season_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_seasons')
                field: season_id
      - name: team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id

  - name: fct_scorers
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [competition_code, season_id, player_id]
    columns:
      - name: competition_code
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_competitions')
                field: competition_code
      - name: season_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_seasons')
                field: season_id
      - name: player_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_players')
                field: player_id
      - name: team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id

  - name: mart_standings_over_time
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [team_id, competition_code, season_id, group_name, matchday]
    columns:
      - name: team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: competition_code
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_competitions')
                field: competition_code
      - name: season_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_seasons')
                field: season_id
      - name: matchday
        tests: [not_null]
      - name: position
        tests: [not_null]

  - name: mart_team_form
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [team_id, competition_code]
    columns:
      - name: team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: competition_code
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_competitions')
                field: competition_code
      - name: last_5_results
        tests: [not_null]

  - name: mart_streaks
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [team_id, competition_code]
    columns:
      - name: team_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: competition_code
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_competitions')
                field: competition_code
      - name: current_win_streak
        tests: [not_null]
      - name: current_unbeaten_streak
        tests: [not_null]
      - name: longest_win_streak
        tests: [not_null]
      - name: longest_unbeaten_streak
        tests: [not_null]

  - name: mart_head_to_head
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [team_a_id, team_b_id]
      - dbt_utils.expression_is_true:
          arguments:
            expression: "team_a_id < team_b_id"
    columns:
      - name: team_a_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: team_b_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_teams')
                field: team_id
      - name: matches_played
        tests: [not_null]

unit_tests:
  - name: finished_match_produces_correct_result_and_score_swap
    description: >
      A 2-1 home win must produce a W row for the home team and an L row
      for the away team, with goals_for/goals_against swapped per side --
      not just copied.
    model: fct_team_matches
    given:
      - input: ref('fct_matches')
        rows:
          - {match_id: 1, home_team_id: 10, away_team_id: 20, status: 'FINISHED',
             full_time_home: 2, full_time_away: 1}
    expect:
      rows:
        - {match_id: 1, team_id: 10, opponent_team_id: 20, is_home: true,
           goals_for: 2, goals_against: 1, result: 'W'}
        - {match_id: 1, team_id: 20, opponent_team_id: 10, is_home: false,
           goals_for: 1, goals_against: 2, result: 'L'}

  - name: unfinished_match_has_no_result
    description: >
      A TIMED match has no scores yet. result must be null on both derived
      rows rather than a misleading W/D/L computed by comparing two nulls.
    model: fct_team_matches
    given:
      - input: ref('fct_matches')
        rows:
          - {match_id: 2, home_team_id: 10, away_team_id: 20, status: 'TIMED',
             full_time_home: null, full_time_away: null}
    expect:
      rows:
        - {match_id: 2, team_id: 10, opponent_team_id: 20, is_home: true,
           goals_for: null, goals_against: null, result: null}
        - {match_id: 2, team_id: 20, opponent_team_id: 10, is_home: false,
           goals_for: null, goals_against: null, result: null}

  - name: finished_match_with_null_scores_has_no_result
    description: >
      status = FINISHED alone does not guarantee a scoreline. Under SQL's
      three-valued logic, comparing a null goals_for/against falls through
      any CASE that only checks status <> 'FINISHED', fabricating a 'D' for
      a match with no recorded score. Verified 0 such rows exist in the real
      warehouse today, but the fix must not depend on that staying true.
    model: fct_team_matches
    given:
      - input: ref('fct_matches')
        rows:
          - {match_id: 3, home_team_id: 10, away_team_id: 20, status: 'FINISHED',
             full_time_home: null, full_time_away: null}
    expect:
      rows:
        - {match_id: 3, team_id: 10, opponent_team_id: 20, is_home: true,
           goals_for: null, goals_against: null, result: null}
        - {match_id: 3, team_id: 20, opponent_team_id: 10, is_home: false,
           goals_for: null, goals_against: null, result: null}

  - name: awarded_match_uses_winner_not_goals
    description: >
      AWARDED matches (forfeited/technically-decided) have a real outcome
      but no goals -- verified 3 such matches exist in the live warehouse
      today, all previously getting result = null. The API's winner field
      (HOME_TEAM/AWAY_TEAM/DRAW) carries the real outcome; result must be
      reframed from each team's own is_home perspective, the same way goals
      already are.
    model: fct_team_matches
    given:
      - input: ref('fct_matches')
        rows:
          - {match_id: 4, home_team_id: 10, away_team_id: 20, status: 'AWARDED',
             winner: 'HOME_TEAM', full_time_home: null, full_time_away: null}
          - {match_id: 5, home_team_id: 10, away_team_id: 20, status: 'AWARDED',
             winner: 'DRAW', full_time_home: null, full_time_away: null}
    expect:
      rows:
        - {match_id: 4, team_id: 10, opponent_team_id: 20, is_home: true,  result: 'W'}
        - {match_id: 4, team_id: 20, opponent_team_id: 10, is_home: false, result: 'L'}
        - {match_id: 5, team_id: 10, opponent_team_id: 20, is_home: true,  result: 'D'}
        - {match_id: 5, team_id: 20, opponent_team_id: 10, is_home: false, result: 'D'}

  - name: an_ongoing_streak_counts_from_the_most_recent_break
    description: >
      Chronological results W, W, L, D, W. The most recent match is a win,
      but it is its own 1-match island (the D right before it broke any
      win streak) -- current_win_streak must be 1, not 3. The D+W pair at
      the end IS one unbeaten island, so current_unbeaten_streak is 2.
      longest_win_streak stays 2 (the W,W at the start), never overtaken.
    model: mart_streaks
    given:
      - input: ref('fct_team_matches')
        rows:
          - {team_id: 900, competition_code: 'PL', kickoff_date_utc: '2024-01-01', result: 'W'}
          - {team_id: 900, competition_code: 'PL', kickoff_date_utc: '2024-01-02', result: 'W'}
          - {team_id: 900, competition_code: 'PL', kickoff_date_utc: '2024-01-03', result: 'L'}
          - {team_id: 900, competition_code: 'PL', kickoff_date_utc: '2024-01-04', result: 'D'}
          - {team_id: 900, competition_code: 'PL', kickoff_date_utc: '2024-01-05', result: 'W'}
    expect:
      rows:
        - {team_id: 900, competition_code: 'PL', current_win_streak: 1,
           current_unbeaten_streak: 2, longest_win_streak: 2, longest_unbeaten_streak: 2}

  - name: a_broken_streak_reports_zero_current_but_keeps_the_longest
    description: >
      Chronological results W, W, W, L. The most recent match is a loss,
      so both current streaks must be 0 -- not the length of the win run
      that just ended. longest_win_streak and longest_unbeaten_streak both
      stay 3, because "longest" is not "current".
    model: mart_streaks
    given:
      - input: ref('fct_team_matches')
        rows:
          - {team_id: 901, competition_code: 'PL', kickoff_date_utc: '2024-01-01', result: 'W'}
          - {team_id: 901, competition_code: 'PL', kickoff_date_utc: '2024-01-02', result: 'W'}
          - {team_id: 901, competition_code: 'PL', kickoff_date_utc: '2024-01-03', result: 'W'}
          - {team_id: 901, competition_code: 'PL', kickoff_date_utc: '2024-01-04', result: 'L'}
    expect:
      rows:
        - {team_id: 901, competition_code: 'PL', current_win_streak: 0,
           current_unbeaten_streak: 0, longest_win_streak: 3, longest_unbeaten_streak: 3}
